# Example: MCP side container paired with the upstream oleduc CouchDB container.
#
# Usage:
#   cp docker-compose.example.yml docker-compose.yml
#   cp .env.example .env
#   docker compose up -d
#
# The MCP container uses the published GHCR image by default. For local
# development against this repo's Dockerfile, comment out `image:` and
# uncomment `build:` below.

services:
  couchdb:
    image: oleduc/docker-obsidian-livesync-couchdb:latest
    container_name: obsidian-couchdb
    restart: unless-stopped
    ports:
      - "${COUCHDB_PORT:-5984}:5984"
    environment:
      - SERVER_DOMAIN=${SERVER_DOMAIN:-localhost}
      - COUCHDB_USER=${COUCHDB_USER:-admin}
      - COUCHDB_PASSWORD=${COUCHDB_PASSWORD:?required}
      - COUCHDB_DATABASE=${COUCHDB_DATABASE:-obsidian}
    volumes:
      - ${COUCHDB_DATA:-./couchdb-data}:/opt/couchdb/data

  mcp:
    # Published image (default): `latest` tracks the most recent release.
    # For reproducible deployments, pin a specific version, e.g. `:1.0.0`.
    image: ghcr.io/bestonephoenix/obsidian-livesync-couchdb-mcp-sidecontainer:latest
    # Local development / testing changes in this repo: comment out the
    # `image:` line above and uncomment `build:` instead.
    # build: .
    container_name: obsidian-mcp
    restart: unless-stopped
    depends_on:
      - couchdb
    ports:
      - "${MCP_PORT:-8000}:8000"
    environment:
      - OBSIDIAN_COUCH_URL=http://couchdb:5984
      - OBSIDIAN_COUCH_USER=${COUCHDB_USER:-admin}
      - OBSIDIAN_COUCH_PASS=${COUCHDB_PASSWORD:?required}
      - OBSIDIAN_COUCH_DB=${COUCHDB_DATABASE:-obsidian}
      - LIVESYNC_PASSPHRASE=${LIVESYNC_PASSPHRASE:-}
